# Peoples-Mandate

TODO:
<br>i. Authentication and profile system (identity validator)

- LOGIN: use siwe library for login using eth address
- SIGNUP:
  - mock oauth digilocker sign in, maybe use DECO protocol
  - use a validator salt to hash with adhaar to generate a profile id
  - store the wallet address -> profile id mapping (only single address supported rn, although changeable).

<br>ii. postgres DB
<br>iii. ui/ux
